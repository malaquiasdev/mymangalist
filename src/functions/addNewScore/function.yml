addNewScore:
  handler: src/functions/addNewScore/main.handler
  events:
    - http:
        path: /mymangalist/user/mangas/{slug}/scores
        method: patch
        cors: true
        request:
          schemas:
            application/json: ${file(src/functions/addNewScore/add_score_request.json)}