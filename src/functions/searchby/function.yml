searchby:
  handler: src/functions/searchby/main.handler
  events:
    - http:
        path: /mymangalist/user/items/search
        method: get
        cors: true
        private: ${env:PRIVATE_ENDPOINT, false}